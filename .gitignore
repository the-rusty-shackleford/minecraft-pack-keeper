# --- Gradle / build output ---------------------------------------------
build/
.gradle/
out/

# --- Dev run directory --------------------------------------------------
# `runClient` / `runServer` write a whole Minecraft installation here, and
# the booth's fixtures and photos land in run/booth.
run/

# --- Editors / OS -------------------------------------------------------
.idea/
*.iml
.vscode/
.DS_Store
*.swp
